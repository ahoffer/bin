#!/usr/bin/env bash
#
# Quick build. Wraps mvn with sensible defaults for local development,
# running the code formatter and license header fixer before the
# requested goals. Tests are skipped unless -t is passed.
#
# Module-specific behavior triggered by working directory:
#   docs/     Delegates to docbuild, bypassing maven entirely.
#   frontend/ Runs yarn prettier before maven to prevent build failures.
#
# Worktree support: Antora cannot resolve content from a git worktree,
# so full builds from the project root exclude the docs module from
# maven and build it separately via docbuild afterward.

skip_tests=true
threads="-T6"
mvn_args=()
goals=()

# Target architecture for docker image builds. Empty means native,
# no cross-compilation. Set via -a flag or CONTAINER_ARCH env var
# to cross-compile via buildx after the Maven build.
arch="${CONTAINER_ARCH:-}"

require_value() {
    local opt="$1"
    local val="${2:-}"
    if [[ -z "$val" || "$val" == -* ]]; then
        echo "Error: $opt requires a value." >&2
        echo "Usage: qb [OPTIONS] [MAVEN-GOALS]" >&2
        exit 2
    fi
}

while [[ $# -gt 0 ]]; do
    case "$1" in
        -t|--test) skip_tests=false ;;
        -1|--single-thread) threads="" ;;
        -a|--arch) require_value "$1" "${2:-}"; arch="$2"; shift ;;
        -h|--help)
            echo "Usage: qb [OPTIONS] [MAVEN-GOALS]"
            echo "Options:"
            echo "  -t              Run tests"
            echo "  -1              Single-thread maven build"
            echo "  -a <platform>   Target architecture (default: auto-detect host)"
            echo "  -h              Show this help"
            exit 0
            ;;
        -T)
            require_value "$1" "${2:-}"
            if [[ ! "$2" =~ ^[0-9]+$ ]]; then
                echo "Error: -T requires a numeric thread count (e.g. 6 or 8)." >&2
                exit 2
            fi
            threads="-T$2"
            shift
            ;;
        -am|-amd) mvn_args+=("$1") ;;
        -pl|-rf|-P|-f|-s|-gs)
            require_value "$1" "${2:-}"
            mvn_args+=("$1" "$2")
            shift
            ;;
        -*) mvn_args+=("$1") ;;
        *) goals+=("$1") ;;
    esac
    shift
done

[[ ${#goals[@]} -eq 0 ]] && goals=(install)
if $skip_tests; then
    mvn_args+=("-DskipTests" "-Djacoco.skip=true")
fi

git_root=$(git rev-parse --show-toplevel 2>/dev/null) || true

# Returns true when PWD is inside the frontend module or at the project
# root that contains a frontend module.
is_frontend_context() {
    [[ -n "$git_root" ]] || return 1
    [[ -d "$git_root/frontend" ]] || return 1
    local resolved_pwd rel
    resolved_pwd=$(realpath "$PWD")
    rel="${resolved_pwd#"$git_root"}"
    rel="${rel#/}"
    [[ -z "$rel" || "$rel" == "frontend" || "$rel" == frontend/* ]]
}

prepare_frontend_if_needed() {
    is_frontend_context || return 0
    (
        cd "$git_root/frontend" || exit 1
        [[ -d node_modules ]] || yarn install
        yarn fix
    )
}

is_docs_context() {
    [[ -n "$git_root" ]] || return 1
    local resolved_pwd rel
    resolved_pwd=$(realpath "$PWD")
    rel="${resolved_pwd#"$git_root"}"
    rel="${rel#/}"
    [[ "$rel" == "docs" || "$rel" == docs/* ]]
}

# A worktree has a .git file instead of a .git directory.
is_worktree() {
    [[ -n "$git_root" ]] && [[ -f "$git_root/.git" ]]
}

is_project_root() {
    [[ -n "$git_root" ]] && [[ "$(realpath "$PWD")" == "$(realpath "$git_root")" ]]
}

is_projects_docker_context() {
    local resolved_pwd rel
    resolved_pwd=$(realpath "$PWD")
    [[ "$resolved_pwd" == "$HOME/projects/"* ]] || return 1
    rel="${resolved_pwd#"$HOME/projects/"}"
    [[ "/$rel/" == *"/docker/"* ]]
}

ensure_docker_daemon_if_needed() {
    is_projects_docker_context || return 0

    if ! command -v docker >/dev/null 2>&1; then
        echo "Error: docker is required for builds under ~/projects/**/docker, but the docker CLI is not installed." >&2
        exit 1
    fi

    if ! docker info >/dev/null 2>&1; then
        echo "Error: docker daemon is unavailable for builds under ~/projects/**/docker." >&2
        if [[ -n "${DOCKER_HOST:-}" ]]; then
            echo "Current DOCKER_HOST: ${DOCKER_HOST}" >&2
        fi
        echo "Start Colima/Docker first, then rerun qb." >&2
        exit 1
    fi
}

# The docs module uses Antora, not javac, so skip maven entirely.
if is_docs_context; then
    exec "$HOME/bin/cxappdev/docbuild" "$@"
fi

ensure_docker_daemon_if_needed

prepare_frontend_if_needed || exit 1

# Only exclude docs from the full root build. Submodule builds like
# docker/edge or backend/plugins should not be affected.
if is_worktree && is_project_root && [[ -d "$git_root/docs" ]]; then
    mvn_args+=("-pl" "!docs")
    build_docs_separately=true
fi

# Always format and fix license headers before compiling.
goals=(com.spotify.fmt:fmt-maven-plugin:format license:format "${goals[@]}")

mvn $threads "${mvn_args[@]}" "${goals[@]}" || exit $?

# When the target architecture differs from the host, rebuild Docker
# images using buildx. In a docker module directory, rebuilds just
# that component. At the project root, finds all docker modules that
# Maven built and rebuilds each.
if [[ -n "$arch" ]]; then
    source "$HOME/bin/cx.lib"
    if needs_cross_build "$arch"; then
        require_project_root
        if is_projects_docker_context; then
            local_comp=$(basename "$(realpath "$PWD")")
            buildx_rebuild "$PROJECT_ROOT" "$local_comp" "$arch" || exit $?
        elif is_project_root; then
            for assembled in "$PROJECT_ROOT"/docker/*/target/docker; do
                [[ -d "$assembled" ]] || continue
                local_comp=$(basename "$(dirname "$(dirname "$assembled")")")
                buildx_rebuild "$PROJECT_ROOT" "$local_comp" "$arch" || exit $?
            done
        fi
    fi
fi

if [[ "${build_docs_separately:-}" == true ]]; then
    ( cd "$git_root/docs" && "$HOME/bin/cxappdev/docbuild" )
fi
