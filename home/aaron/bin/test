#!/bin/bash

jre=$(find "/usr/lib/jvm" java)
jdk=$(find "/usr/lib/jvm" java)

echo $jre
echo $jdk

#all=printf '%s\n%s\n' "$jre" "$jdk"

#while IFS= read -r line; do
#    echo "Line: $line"
#done <<< "$jre"
