#!/bin/bash
# Askpass helper for use with SUDO_ASKPASS.
# Shows a zenity password dialog so the user can approve or deny
# each sudo invocation from non-interactive contexts like Claude Code.

prompt="${1:-Password:}"

exec zenity --password \
    --title="sudo authorization requested" \
    --text="$prompt" \
    --timeout=60 2>/dev/null
